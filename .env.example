# Clave de la API de Anthropic. Es OPCIONAL:
# sin ella, /api/generar-bloque sirve bloques del banco de reserva
# (lib/banco.ts) y la demo funciona igual.
# Para generación real, copia este archivo a .env.local y rellena la clave.
ANTHROPIC_API_KEY=
